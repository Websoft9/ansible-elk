# global
region: "0"

common_install_docker: True
docker_install: False

# docker
docker_applications:
  - elasticsearch

init_docker:
  elk:
    admin_username: user@domain.com
    admin_password: "changeme"
    service_after: "docker.service"
    compose_path: "/data/wwwroot/elk/docker-compose.yml"
    compose_commands:
      - sudo sed -i "s/elk/$new_password/g" /data/wwwroot/elk/docker-compose.yml
 